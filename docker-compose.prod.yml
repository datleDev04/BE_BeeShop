services:
  app:
    container_name: bee_mely
    build:
      context: .
      dockerfile: Dockerfile.prod
    environment:
      - NODE_ENV=production
    ports:
      - '3000:3000'
    command: npm start
